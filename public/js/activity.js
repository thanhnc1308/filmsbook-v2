/**
 * func handle event click a nav item
 * set active nav item
 * @author NCThanh
 */
function onNavItemClick(el) {
    setActiveClass(el, 'nav-item');
}

/**
 * func handle event click a nav item
 * set active nav item
 * @author NCThanh
 */
function onTabItemClick(el) {
    setActiveClass(el, 'tab-item');
}